#include<fcntl.h >
#include<stdio.h>

int main(void)
{
	char buffer[1]={0};
	int fd ;
	fd=open("/dev/char_tmp", O_RDWR);
	write(fd, buffer, 1, NULL);
	read (fd, buffer, 1, NULL);
	close(fd);
	return 0;
}
