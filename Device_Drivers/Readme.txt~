#Run instructions (run as root)

1) load modules
insmod memory.ko
insmod hello_world.ko

2) check if the modules loaded correctly
lsmod

3) write data to the device; it will store the last character
echo -n abc >/dev/memory

4) check content
cat /dev/memory

5) finally remove modules
rmmod memory
rmmod hello_world